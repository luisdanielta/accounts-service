package handlers

import "net/http"

func LoginGet(w http.ResponseWriter, r *http.Request) {
	/*
		| TASK LIST |
		1. Check if user is already logged in
		2. If user is logged in, redirect to home page
		3. If user is not logged in, render login page
	*/
}
